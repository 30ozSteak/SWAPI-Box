export const parseFilm = film => {
  const modifiedObj = {
    title: film.title,
    openingCrawl: film.openingCrawl,
    episode: film.episode,
    date: film.release_date
  };
  return modifiedObj;
};

// parsePeople

// parsePlanets

// parseVehicles
